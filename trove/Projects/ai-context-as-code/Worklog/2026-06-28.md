---
type: worklog
title: "2026-06-28"
description: "Worklog for starting the ACAC first instance scaffold"
status: active
created: 2026-06-28
updated: 2026-06-29
visibility: public
id: UzbYw-l2kg

---

# 2026-06-28

This worklog records the first repository scaffold and read-only reader for the ACAC first instance.
The work starts from public-safe source notes, repo-local generated agent entries, validation, metadata build, and static output.
External runtime integrations remain outside this implementation unit.

## 02:12 — Korean Reading Mode and English Canonical Source

이 항목은 reset 전 ACAC repo에서 진행된 public reader 실험이에요.
한국어 읽기 화면을 추가하되, agent와 자동화가 읽는 원본은 English canonical source로 두는 기준을 잡았어요.

- `notes/`를 canonical English source로 유지했어요.
- `i18n/ko/notes/`에 한국어 읽기 copy를 추가했어요.
- site UI에 KO/EN 전환과 `?lang=en` 진입을 붙였어요.
- build metadata와 validator가 한국어 번역 누락과 번역 wikilink 문제를 볼 수 있게 했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `4b7c00d`로 확인했어요.

## 02:18 — Unnumbered Note Tree and Graphite Theme

reset 전 reader의 note tree에서 숫자 prefix를 걷어내고, 시각 톤을 더 차분한 graphite 계열로 바꿨어요.
화면에는 번호를 노출하지 않되, 내부 정렬 순서는 유지하는 방향이었어요.

- `00-start`, `10-principles` 같은 번호 prefix를 제거했어요.
- 한국어 번역 경로도 같은 구조로 맞췄어요.
- build sorting으로 logical order를 유지했어요.
- README와 설계 노트에 repo 정체성을 제품 repo와 첫 context corpus로 정리했어요.
- dark theme를 navy/slate에서 graphite 중심으로 조정했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- dashboard 렌더링을 headless Chrome으로 확인했어요.
- Obsidian worklog와 Daily pointer, git commit `a9cc727`로 확인했어요.

## 02:47 — Mobile Navigation QA Fixes

reset 전 mobile Safari 기준으로 topbar와 interaction 문제를 고쳤어요.
작은 화면에서 언어 전환, edit action, preview 동작이 서로 겹치지 않게 정리한 작업이에요.

- topbar action을 별도 container로 묶었어요.
- mobile floating edit button을 topbar 내부 action으로 바꿨어요.
- mobile에서는 theme button을 숨겨 topbar 겹침을 줄였어요.
- 링크 이동 시 anchor가 없으면 page top으로 이동하게 했어요.
- hover preview는 pointer가 있는 desktop 환경에서만 작동하게 했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `798030d`로 확인했어요.

## 03:07 — Graph UX and Content Structure Plan

reset 전 reader에서 graph를 단순 장식이 아니라 문서 관계를 읽는 `Context Map`으로 바꾸는 방향을 잡았어요.
동시에 다음 content folder 구조를 설계 문서로 남겼어요.

- `Local Graph` 중복 노출을 제거했어요.
- 하단 graph 영역을 `Context Map`으로 바꾸고 related/full map 전환을 붙였어요.
- graph insight card가 현재 문서와 관련 문서의 연결 이유를 설명하게 했어요.
- content structure plan을 English/Korean copy로 작성했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- headless Chrome으로 dashboard와 project note 렌더링을 확인했어요.
- Obsidian worklog와 Daily pointer, git commit `c428f84`로 확인했어요.

## 03:18 — Dashboard Mode and Agent Runtime References

reset 전 홈 화면을 일반 문서 화면이 아니라 운영 대시보드로 분리했어요.
agent-facing 설정 문서도 runtime 자체가 아니라 agent config surface의 index로 정리했어요.

- dashboard route에서 note tree, context panel, graph, edit button을 숨겼어요.
- health, review queue, recent notes, hub notes 중심으로 홈을 다시 배치했어요.
- agent runtime reference 문서를 추가했어요.
- review queue 기준을 최근 변경 검토 항목까지 넓혔어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `b3d50ac`로 확인했어요.

## 03:33 — Dashboard Polish and Korean Writing Voice Alignment

reset 전 dashboard의 폭과 한국어 문체를 정리했어요.
한국어 copy가 번역투로 보이지 않도록 label과 문장 흐름을 다듬은 작업이에요.

- desktop dashboard가 더 넓은 화면을 쓰도록 max-width 중심 레이아웃을 풀었어요.
- dashboard 제목과 상태 카드 label을 자연스럽게 정리했어요.
- 한국어 문서 31개를 훑어 어색한 표현을 고쳤어요.
- writing voice와 어색한 표현 검증 규칙을 note convention 쪽에 추가했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `50414f9`로 확인했어요.

## 03:45 — Product UI Redesign Pass

reset 전 ACAC reader를 더 제품형 UI로 다듬었어요.
문서 랜딩 페이지보다 반복 사용 가능한 작업 화면처럼 보이게 하는 데 초점을 뒀어요.

- dark theme 색을 더 절제된 graphite 바탕과 accent 중심으로 맞췄어요.
- topbar를 brand, search, action 순서로 정리했어요.
- dashboard hero를 낮추고 health, review queue, recent changes 중심으로 바꿨어요.
- sidebar, context panel, graph panel의 border, padding, hover 상태를 맞췄어요.
- mobile topbar를 단순 flex row로 바꿨어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `5ca2405`로 확인했어요.

## 03:48 — Navbar and Dashboard Alignment Pass

reset 전 navbar와 dashboard의 좌우 기준선을 맞췄어요.
검색창이 brand/action 폭에 끌려가지 않고 중앙에서 안정적으로 보이게 한 작업이에요.

- desktop topbar를 3열 grid로 바꿨어요.
- 검색창을 가운데 열에 고정했어요.
- topbar와 dashboard가 같은 inset 값을 쓰게 했어요.
- 검색 입력창 높이를 낮춰 navbar 두께를 줄였어요.
- mobile dashboard의 시작선도 같은 기준으로 맞췄어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `1354fde`로 확인했어요.

## 03:58 — Modal Search and Agent Config Framing

reset 전 검색 UX를 짧은 navbar trigger와 중앙 modal 중심으로 바꿨어요.
agent 설정 표현도 독자 runtime이 아니라 일반 agent config surface로 정리했어요.

- navbar 검색 입력창을 modal trigger로 줄였어요.
- 검색 modal에 command group, note group, snippet, keyboard selection을 추가했어요.
- interface design direction 문서를 추가했어요.
- `Agent runtime` 표현을 `Agent config` 중심으로 바꿨어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `0527a79`로 확인했어요.

## 04:10 — Agent Config Surfaces and Search Ranking

reset 전 agent-facing 설정 문서와 검색 ranking을 실제 구현에 맞췄어요.
검색은 단순 텍스트 검색이 아니라 title, path, type, body, backlink, recent 상태를 섞어 정렬하게 했어요.

- root `AGENTS.md`와 `CLAUDE.md`를 추가했어요.
- `skills/`와 `memory/` 폴더를 추가했어요.
- agent runtime 문서를 compatibility index로 설명했어요.
- 검색 trigger에 shortcut 표시를 넣고 modal ranking을 보강했어요.
- implementation roadmap review 문서를 추가했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `93233ff`로 확인했어요.

## 12:23 — Roadmap Completion and QA Tooling Pass

reset 전 승인 없이 처리 가능한 남은 roadmap 항목을 마무리했어요.
검색, browser patch export, context map insight, import dry-run report, viewport QA가 한 번에 정리된 작업이에요.

- search modal이 generated `search-index.json`를 실제 ranking에 쓰게 했어요.
- search index에 path, heading, backlink/link 수, 한국어 읽기 텍스트를 포함했어요.
- browser patch export에 변경 section 목록과 review block을 추가했어요.
- context map insight가 연결 이유를 설명하게 했어요.
- viewport screenshot과 DOM smoke check용 QA script를 추가했어요.
- README와 roadmap/design 문서를 갱신했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `1441a6c`로 확인했어요.

## 15:25 — Note-Level Language Switching

reset 전 언어 전환 정책을 전역 UI 기준에서 note body 기준으로 바꿨어요.
기본 제품 chrome은 English로 유지하고, 개별 note 본문에서만 KO/EN 읽기 전환을 두는 방향이에요.

- 기본 언어를 English로 되돌렸어요.
- dashboard와 workspace chrome은 English로 유지했어요.
- home 화면에서는 KO/EN toggle을 숨겼어요.
- sidebar, search result, graph, backlink는 English source title을 쓰게 했어요.
- 검색은 한국어 번역 텍스트도 함께 찾도록 유지했어요.
- README와 interface design note에 정책을 기록했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `8e22f5a`로 확인했어요.

## 15:39 — Fixed Desktop Chrome During Note Scroll

reset 전 문서 하단으로 스크롤할 때 topbar, left tree, right context panel이 사라지는 문제를 고쳤어요.
앱 shell은 viewport에 고정하고, 가운데 workspace만 스크롤되게 만든 작업이에요.

- desktop wide layout에서 app shell을 viewport에 고정했어요.
- workspace만 내부 스크롤을 갖도록 바꿨어요.
- context map을 본문 흐름 안으로 옮겼어요.
- note route 변경 시 workspace scroll도 맨 위로 초기화했어요.
- QA script에 scrolled state capture를 추가했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `f6a9d7c`로 확인했어요.

## 15:48 — Moved Context Map Into Right Sidebar

reset 전 Context Map을 본문 하단에서 오른쪽 sidebar로 옮겼어요.
문서 하단까지 내려가지 않아도 관계 정보를 계속 볼 수 있게 한 UI 변경이에요.

- 본문 하단 Context Map section을 제거했어요.
- 오른쪽 context panel에 compact Context Map을 배치했어요.
- graph를 sidebar 폭에 맞는 compact SVG로 렌더링했어요.
- desktop `.app-shell` 고정을 더 단단하게 잠갔어요.
- QA script가 문서 하단 앵커 상태를 capture하게 했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- Obsidian worklog와 Daily pointer, git commit `0892baa`로 확인했어요.

## 15:51 — Legacy Knowledge Base References Imported

reset 전 private staging에 있던 legacy knowledge base references를 private project reference로 흡수했어요.
이 repo의 publishable corpus에는 원문을 넣지 않고, public-safe rewrite 후보만 남기는 기준을 확인한 작업이에요.

- legacy knowledge base reference를 private project reference 위치로 보존했어요.
- public notes에는 private 원문을 직접 복사하지 않았어요.
- import dry-run report는 private staging 쪽에만 남겼어요.
- 흡수 완료된 raw temp originals는 정리했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.
- repo public payload에는 private reference 원문을 넣지 않은 것으로 기록되어 있어요.

## 15:54 — Tightened Default Horizontal Spacing

reset 전 reader의 기본 좌우 여백을 줄였어요.
dashboard와 note body가 더 실제 작업 화면처럼 넓게 쓰이도록 조정했어요.

- 공통 app inset 값을 낮췄어요.
- document workspace 좌우 padding을 낮췄어요.
- mobile 기본 inset도 줄였어요.
- GitHub Pages cache key를 갱신했어요.

검증:

- 당시 `python3 scripts/review_context.py`를 실행했어요.
- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- Pages deploy success를 확인했어요.
- Obsidian worklog와 Daily pointer, git commit `84ff4bf`로 확인했어요.

## 15:58 — Reduced Desktop Horizontal Padding Again

reset 전 desktop reader의 좌우 여백을 한 번 더 줄였어요.
큰 화면에서 문서와 작업 패널이 과하게 좁아 보이지 않게 한 조정이에요.

- desktop app inset 최대값을 더 낮췄어요.
- document workspace 좌우 padding 최대값을 더 낮췄어요.
- GitHub Pages cache key를 갱신했어요.

검증:

- 당시 `node --check site/app.js`를 실행했어요.
- 당시 `git diff --check`를 실행했어요.
- 당시 `python3 scripts/qa_viewports.py`를 실행했어요.
- Pages deploy success를 확인했어요.
- Obsidian worklog와 Daily pointer, git commit `ac32a31`로 확인했어요.

## 16:00 — Domain Candidates for ACAC Rename

reset 전 `acac.xx` 형태의 공개 도메인 후보를 조사했어요.
developer tool과 context-as-code 의미가 잘 맞는 후보를 public launch 관점에서 정리한 작업이에요.

- `acac.sh`를 포함한 여러 developer-oriented TLD 후보를 확인했어요.
- 이미 등록된 주요 TLD 후보와 사용 가능한 후보를 분리했어요.
- CLI, script, agent command 맥락에 맞는 도메인을 우선순위로 봤어요.

검증:

- 당시 RDAP/WHOIS 기반 조회를 실행했어요.
- Obsidian worklog와 Daily pointer로 확인했어요.

## 16:03 — Domain Decision Recorded as acac.sh

reset 전 공개 도메인을 `acac.sh`로 정하는 결정을 private project decision으로 기록했어요.
CLI와 script 중심 프로젝트 정체성이 가장 잘 드러난다는 판단이었어요.

- `acac.sh`를 1차 공개 도메인으로 결정했어요.
- 구매 전 premium/reserved 여부와 가격을 확인해야 한다고 남겼어요.
- 도메인 선택 근거를 private project decision으로 보존했어요.

검증:

- 당시 `whois acac.sh`를 실행했어요.
- Obsidian worklog와 Daily pointer로 확인했어요.

## 16:07 — GitHub Pages Custom Domain Configured

reset 전 GitHub Pages custom domain을 `acac.sh` 기준으로 설정했어요.
이후 Workers static assets로 배포 경로가 바뀌었지만, 당시 public deploy 단계로 남아야 하는 기록이에요.

- GitHub Pages custom domain을 `acac.sh`로 설정했어요.
- `site/CNAME`을 추가했어요.
- custom domain 설정 commit을 push하고 Pages workflow success를 확인했어요.
- DNS record 후속 작업을 기록했어요.

검증:

- 당시 publish safety, validate, build, review scripts를 실행했어요.
- GitHub Pages workflow와 health API를 확인했어요.
- Obsidian worklog와 Daily pointer, git commit `7391cbb`로 확인했어요.

## 16:08 — Reference-Driven ACAC Gap Review

reset 전 legacy reference를 기준으로 ACAC public reader에 남은 gap을 검토했어요.
private 원문을 옮기는 대신, 공개 가능한 구현 후보와 문서 후보만 분리하는 단계였어요.

- legacy reference에서 제품화에 필요한 차이를 뽑았어요.
- public-safe하게 반영할 수 있는 후보와 private로 남길 내용을 분리했어요.
- 이후 design/UI 작업의 입력으로 남겼어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 16:12 — Tailwind-Style Product Design Review

reset 전 Tailwind CSS documentation처럼 선명한 제품 문서 UI를 참고하되, ACAC만의 방향을 잡는 검토를 했어요.
브랜드를 복사하지 않고 정보 구조와 사용감을 참고하는 데 초점을 뒀어요.

- 제품 문서형 reader의 정보 배치와 탐색 흐름을 검토했어요.
- ACAC public reader에 맞는 visual direction을 정리했어요.
- 이후 UI overhaul의 입력으로 남겼어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 16:16 — Product Rebranded and Deployed as acac.sh

reset 전 제품 이름과 URL 표현을 `acac.sh` 중심으로 리브랜딩하고 배포했어요.
이 단계는 later reset 전에 운영되던 첫 public product-docs 버전이에요.

- README와 context rule, alias, decision, project notes에서 이름을 `acac.sh`로 맞췄어요.
- English/Korean notes의 product naming을 함께 갱신했어요.
- `why-acac-sh` decision과 `acac-sh-design` 문서를 추가했어요.

검증:

- Obsidian worklog와 Daily pointer, git commit `4531e1e`로 확인했어요.

## 16:35 — acac.sh Cloudflare DNS Checked

reset 전 `acac.sh` DNS를 Cloudflare 기준으로 확인했어요.
GitHub Pages custom domain을 붙인 뒤 public DNS 전파와 certificate 상태를 기다리는 단계였어요.

- Cloudflare DNS 설정 상태를 확인했어요.
- public DNS 전파 대기 상태를 기록했어요.
- 이후 HTTPS 확인 작업의 입력으로 남겼어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 16:38 — Settings Modal for ACAC Site

reset 전 ACAC site에 settings modal을 추가했어요.
reader에서 사용자가 site-level 설정을 다루는 작은 entry를 실험한 작업이에요.

- settings modal UI를 추가했어요.
- site-level control을 reader chrome 안에 넣었어요.
- 기존 read-only public reader 범위 안에서 UI만 조정했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:10 — acac.sh HTTPS Certificate Pending

reset 전 `acac.sh` HTTPS certificate 발급 대기 상태를 확인했어요.
도메인 연결은 진행됐지만 certificate와 HTTPS redirect가 아직 완전히 안정되지 않은 상태였어요.

- HTTPS certificate 발급 상태를 확인했어요.
- 대기 중인 상태와 다음 확인 기준을 기록했어요.
- 이후 HTTPS fix 작업으로 이어졌어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:17 — acac.sh HTTPS Fixed

reset 전 `acac.sh` HTTPS 문제를 해결하고 HTTPS 강제 적용을 확인했어요.
이 기록은 당시 GitHub Pages 기반 public deploy 운영 이력으로 남겨요.

- HTTPS certificate 문제를 해결했어요.
- HTTPS 강제 적용 상태를 확인했어요.
- public site가 HTTPS로 접근되는 것을 확인했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:20 — Tailwind-Style Product UI Overhaul

reset 전 `acac.sh`를 Tailwind-style product docs UI로 크게 개편했어요.
문서 사이트보다 제품 문서 도구처럼 보이게 만드는 overhaul였어요.

- product docs UI 방향으로 layout과 style을 크게 바꿨어요.
- QA viewport script를 보강했어요.
- site app, index, style을 함께 수정했어요.

검증:

- Obsidian worklog와 Daily pointer, git commit `7edcf86`로 확인했어요.

## 21:26 — Tailwind-Style Product UI Deployed

reset 전 product docs UI 개편을 public deploy까지 완료했어요.
이후 21:42에 repo를 비우고 첫 인스턴스 범위를 재정의하기 전 마지막 public UI 배포 단계예요.

- Tailwind-style product docs UI를 배포했어요.
- public site가 새 UI를 반환하는지 확인했어요.
- 다음 단계에서 방향 재검토가 필요하다는 맥락을 남겼어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:42 — Repository Reset to Empty Baseline

ACAC를 처음부터 다시 시작하기 위해 reset 전 repo contents를 비웠어요.
이후 현재 `trove/` 기반 첫 인스턴스가 새 baseline으로 만들어졌어요.

- 기존 notes, i18n, agent-runtime, site, scripts, workflow를 제거했어요.
- product-docs experiment를 접고 empty baseline에서 다시 시작할 수 있게 했어요.
- 이후 current first-instance scaffold의 기준점이 됐어요.

검증:

- Obsidian worklog와 Daily pointer, git commit `d57b1c5`로 확인했어요.

## 21:53 — First-Instance Scope Reset Recorded

ACAC 첫 목표를 jeina local Obsidian의 cloud-readable first instance로 다시 정의했어요.
범용 framework보다 실제 운영 context를 public-safe하게 옮기는 첫 인스턴스에 초점을 맞췄어요.

- first instance 목표를 cloud-based context system으로 재정의했어요.
- public-safe source와 generated output의 경계를 다시 잡았어요.
- external runtime integration은 첫 baseline 밖으로 밀었어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:56 — Context Root Naming Changed From Vault

첫 인스턴스 root content folder 이름을 `vault` 대신 `context`로 바꿨어요.
Obsidian vault 전체를 옮기는 인상을 줄이고, 공개 가능한 context source라는 의미를 살리려는 변경이었어요.

- root naming 후보를 `vault`에서 `context`로 바꿨어요.
- active design 경로와 설명을 새 이름에 맞췄어요.
- private vault와 public source의 경계를 더 분명히 했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 21:57 — Ambiguous Knowledge Folder Removed

모호한 `Knowledge/` 폴더를 유지하지 않고 더 구체적인 공용 원칙, 참고, 조사 영역으로 나누는 결정을 했어요.
새 구조가 사람이 봐도 어떤 문서를 어디에 둘지 알 수 있게 만드는 단계였어요.

- `Knowledge/`라는 넓은 이름을 제거했어요.
- principles, references, research 성격을 구분했어요.
- folder naming을 first-instance source model에 맞췄어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:00 — System Folder Split From Config and Project Research

프로젝트별 references/research와 system/config layer를 분리했어요.
project knowledge와 agent/system operating source가 같은 층에 섞이지 않게 하려는 설계였어요.

- project-specific references와 research를 project folder 아래에 두는 방향을 잡았어요.
- agent/system config 성격의 source는 별도 layer로 분리했어요.
- first-instance folder map을 갱신했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:03 — Daily Context Hub Added

ACAC 첫 인스턴스에 하루 단위 context hub인 `Daily/` 규칙을 추가했어요.
상세 구현 기록은 project worklog에 두고, Daily는 하루 단위 pointer와 이어받을 맥락을 맡게 했어요.

- `Daily/YYYY-MM/YYYY-MM-DD.md` 구조를 설계에 포함했어요.
- Daily와 project worklog의 역할을 분리했어요.
- 하루 단위 Open Context와 Worklog pointer 흐름을 잡았어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:08 — AGENTS and Memory Rules Folded Into System Structure

ACAC 프로젝트 폴더명을 `ai-context-as-code`로 맞추고 agent/memory 운영 규칙을 system 구조에 반영했어요.
repo-local agent entry와 source memory가 같은 구조 안에서 설명되게 만든 작업이에요.

- project folder naming을 `ai-context-as-code`로 맞췄어요.
- AGENTS와 memory 운영 규칙을 system source 구조에 넣었어요.
- root entry가 generated artifact라는 방향을 잡았어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:12 — Special Folders Moved Below Main Context

보조 폴더를 `_config`, `_system`, `_archived`로 바꾸고 main context와 분리했어요.
사용자-facing context와 운영 보조 영역을 reader sidebar에서 다르게 보이게 하려는 설계였어요.

- special folder naming을 underscore 계열로 정리했어요.
- main context와 운영 보조 영역을 sidebar에서 분리하는 방향을 잡았어요.
- `_assets`를 user-facing section으로 노출하지 않는 원칙과 이어지게 했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:17 — Memory Promoted to Main Context

한때 장기 context인 `Memory/`를 main 영역으로 승격하는 방향을 검토했어요.
이후 22:21에 다시 `_config` 아래로 재정리됐지만, 설계 판단 흐름으로 보존해야 하는 단계예요.

- long-term context를 main context로 둘지 검토했어요.
- `_system` 제거 방향을 함께 봤어요.
- user-facing context와 agent-facing source의 경계를 다시 평가했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:21 — Memory Grouped With Skills and Commands

`Memory/`, `Skills/`, `Commands/`를 `_config/` 아래 special contents로 다시 묶었어요.
agent-facing operating source를 user-facing trove와 분리하는 현재 baseline의 직접 선행 결정이에요.

- memory, skills, commands를 `_config/` 아래로 모았어요.
- agent-facing config layer를 main context 밖으로 분리했어요.
- sidebar에서는 special contents로 보이게 하는 방향을 유지했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:29 — Assets Hidden as Internal Storage

첨부 저장소를 user-facing `Assets/`가 아니라 숨김 `_assets/` 내부 storage로 두기로 했어요.
검색과 navigation에서 asset storage가 지식 섹션처럼 보이지 않게 하는 기준이에요.

- `Assets/` user-facing section을 쓰지 않기로 했어요.
- `_assets/`를 internal storage로 두는 방향을 잡았어요.
- navigation/search에서 `_assets`를 제외하는 원칙으로 이어졌어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:32 — Root Folder Named Trove

ACAC root source folder 이름을 `trove/`로 결정했어요.
context source를 단순 vault 복사본이 아니라 선별된 보관소처럼 다루는 현재 repo 계약의 시작점이에요.

- root source folder를 `trove/`로 정했어요.
- active 설계 경로와 설명을 `trove/` 기준으로 바꿨어요.
- 현재 editable source layer naming의 기준이 됐어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:35 — Home Document Removed From Trove

`trove/Home.md`를 만들지 않고 root README와 generated home data가 첫 화면을 맡게 했어요.
source note와 reader home shell의 책임을 분리한 결정이에요.

- `trove/Home.md`를 만들지 않는 기준을 잡았어요.
- root README를 public home summary의 source로 쓰는 방향을 정했어요.
- site routing이 첫 화면을 담당하게 했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:40 — Reference-Based Architecture and Implementation Plan

legacy reference를 public-safe하게 검토해 first-instance architecture와 implementation plan을 작성했어요.
private 원문은 옮기지 않고, 재사용 가능한 구조만 현재 ACAC 설계로 옮긴 작업이에요.

- first-instance architecture design을 작성했어요.
- first implementation plan을 작성했어요.
- legacy knowledge base principle을 public-safe reference로 정리할 준비를 했어요.
- external runtime integration은 baseline 밖으로 두는 범위를 명확히 했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:47 — First-Instance UI and UX Design

ACAC public reader의 첫 화면 구성과 UI/UX design을 작성했어요.
read-only static reader가 어떤 화면과 interaction을 가져야 하는지 구현 전 기준을 남긴 작업이에요.

- home, note, search, missing route의 화면 구성을 잡았어요.
- sidebar, context panel, backlinks, relation preview의 역할을 정리했어요.
- public-safe reader boundary를 design에 반영했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:55 — Real URL Routes and ID Registry Decision

public site를 hash route가 아니라 실제 URL route와 ID registry 기반으로 설계하기로 결정했어요.
파일 이동이 있어도 stable route를 유지하기 위한 기반 결정이에요.

- hash route 대신 direct `/trove/<id>` route를 선택했어요.
- ID registry를 canonical route 유지 장치로 두었어요.
- Cloudflare Pages fallback을 고려한 route design을 기록했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 22:57 — Canonical Trove ID Route

문서 canonical URL을 제품 언어가 드러나는 `/trove/<id>`로 구체화했어요.
ID 생성 스펙과 URL 형태를 구현 가능한 수준으로 정리한 결정이에요.

- canonical route를 `/trove/<id>`로 정했어요.
- ID 생성과 registry preservation 기준을 구체화했어요.
- reader와 generated payload가 같은 route 계약을 쓰게 했어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 23:02 — Decisions Folded Into Design SSOT

accepted decisions를 별도 후보로 남기지 않고 design 하위 source-of-truth 문서에 직접 반영했어요.
구현 시작 전에 설계 문서가 최신 결정을 담도록 정리한 작업이에요.

- accepted decisions를 architecture와 implementation design에 반영했어요.
- 중복 decision 후보를 줄이고 구현 기준을 한 곳에 모았어요.
- 첫 scaffold가 읽을 문서 흐름을 단순하게 만들었어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 23:18 — Final Implementation Spec Aligned

구현 직전 final spec을 Cloudflare Pages, analytics, agent docs sync 기준으로 정리했어요.
이후 23:27 first scaffold가 실제 repo source와 generated output을 만드는 기준이 됐어요.

- deploy 전제와 generated output 경계를 정리했어요.
- agent entry sync 흐름을 구현 기준에 넣었어요.
- analytics token injection을 선택 사항으로 둔 기준을 남겼어요.

검증:

- Obsidian worklog와 Daily pointer로 확인했어요.

## 23:27 — First Instance Scaffold

- Created the initial `trove/`, `site/`, `scripts/`, `data/`, and `dist/` structure.
- Seeded the ACAC project index, daily context, `_config` index, memory index, and agent source documents.
- Added `scripts/sync_agent_docs.py` and generated root `AGENTS.md` and `CLAUDE.md`.
- Added `scripts/validate_trove.py` for frontmatter, H1, summary, visibility, `_assets/`, and ID checks.
- Added `scripts/build_trove.py` for ID registry, metadata JSON, markdown payload, Cloudflare Pages `dist/`, route fallback, and analytics token injection.
- Added the read-only static reader with home, sidebar, note view, search, backlinks, and mobile navigation.
- Added `scripts/serve_dist.py` for local app-shell fallback testing.
- Deferred Cloudflare deploy, first reviewed note migration, MCP, hooks, Claude Code live connection, and automatic memory sync to later implementation units.

## 23:38 — LAN Dev Server Binding

The local dist server was reachable only from the Mac mini because it bound to `127.0.0.1`.
The server script now accepts a `--host` option so the same output can be served on the LAN during manual QA.
This is still a local development helper and does not change deploy behavior.

- Added `--host` to `scripts/serve_dist.py`.
- Verified `0.0.0.0:4173` listens on all interfaces.
- Verified root, search, and note routes still return the app shell through the fallback server.
- Moved the running dev server into the `tmux` session `acac-serve` so it stays alive outside the Codex tool session.

## 23:53 — First Document Migration

The next active-design step was the first document migration.
The ACAC project now has real design notes and one sanitized reference inside `trove/Projects/ai-context-as-code/`.
Raw legacy references were not copied because they contain internal infrastructure details.

- Migrated the active design notes into `trove/Projects/ai-context-as-code/Designs/`.
- Added `trove/Projects/ai-context-as-code/References/legacy-knowledge-base-principles.md` as a public-safe reference.
- Linked the migrated design and reference notes from the project index.
- Improved wikilink validation and backlink extraction so example links inside fenced code blocks are ignored.
- Rebuilt metadata and verified 13 public notes with 0 warnings.
