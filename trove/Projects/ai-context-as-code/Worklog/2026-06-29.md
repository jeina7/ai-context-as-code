---
type: worklog
title: "2026-06-29"
description: "Worklog for ACAC deployment, reader UI, and analytics cleanup"
status: active
created: 2026-06-29
updated: 2026-06-29
visibility: public
id: KLWL70bVad

---

# 2026-06-29

This worklog records ACAC deployment preparation, reader UI polish, and post-deploy cleanup for the first instance.
The work keeps the public-safe static reader aligned with the actual Cloudflare Workers static assets deployment.
External runtime integrations remain outside this implementation unit.

## 00:01 — Cloudflare Pages Deploy Prep

- Checked current official Cloudflare Pages docs for Git integration, build settings, redirects, and Web Analytics.
- Added `.env.example` with the optional `ACAC_CF_WEB_ANALYTICS_TOKEN` variable.
- Added `scripts/check_public_payload.py` to verify public metadata, payload files, `_assets` exclusion, route shape, and required `_redirects`.
- Added `scripts/deploy_check.py` to run agent doc sync, build, public payload checks, and JavaScript syntax checks.
- Added [[cloudflare-pages-deploy-prep]] with dashboard values, analytics options, and predeploy checks.
- Linked the deploy prep reference from the ACAC project index.
- Verified `python3 scripts/deploy_check.py` passes with 16 public notes and 0 warnings.

## 00:15 — Git Integration Selected and Baseline Pushed

ACAC first instance deployment path was narrowed to Cloudflare Git integration.
The baseline repo state was committed and pushed so the external build system could read the source.

- Added the Git integration decision note for Cloudflare Pages setup.
- Updated the deploy prep reference so Direct Upload was not treated as the chosen path.
- Created commit `82a3b81 Build ACAC first instance baseline`.
- Pushed the baseline to `origin/main`.

검증:

- `python3 scripts/deploy_check.py` passed before the baseline commit.
- `origin/main` was confirmed at the pushed baseline commit.
- Evidence was checked through Obsidian worklog, Daily pointer, `git log`, `git show --stat`, and `git show --name-only`.

## 01:25 — Workers Static Assets Deploy Command Fallback

Cloudflare's dashboard flow required a deploy command, so the repo was prepared for Workers Builds static assets as a fallback path.
This kept the previous Pages Git integration decision as context while making the active dashboard path deployable.

- Added `wrangler.jsonc` for Workers static assets from `./dist`.
- Set `not_found_handling: single-page-application` for direct note/search routes.
- Added [[2026-06-29-cloudflare-workers-static-assets-fallback]].
- Added [[cloudflare-workers-static-assets-deploy-prep]] with dashboard values.
- Recorded preview build settings in the Workers reference and decision notes.
- Created and pushed commits `4d5d925` and `5d96039`.

검증:

- `python3 scripts/deploy_check.py` passed with 19 public notes and 0 warnings.
- Push to `origin/main` was confirmed.
- Evidence was checked through Obsidian worklog, Daily pointer, `git log`, `git show --stat`, and `git show --name-only`.

## 01:28 — Workers First Build Failure Follow-Up

The first Workers Builds run failed, so the deploy config was checked against local validation and Wrangler dry-run behavior.
The likely issue was a future compatibility date from Cloudflare's API perspective.

- Changed `wrangler.jsonc` compatibility date from `2026-06-29` to `2026-06-28`.
- Added the custom domain attachment order to the Workers deploy prep reference.
- Created and pushed commit `d81f645 Pin Workers compatibility date for deploy`.

검증:

- `python3 scripts/deploy_check.py` passed with 19 public notes and 0 warnings.
- `npx wrangler deploy --dry-run` passed locally.
- Evidence was checked through Obsidian worklog, Daily pointer, `git log`, `git show --stat`, and `git show --name-only`.

## 01:33 — Custom Domain Attachment Checked

After `acac.sh` and `www.acac.sh` were attached as Workers custom domains, external responses were checked.
The apex and www hostnames were not yet serving the same intended ACAC build.

- Confirmed the apex hostname still returned the previous GitHub Pages response.
- Confirmed note and search routes on the apex hostname were still not the intended Workers app shell.
- Confirmed `www.acac.sh` returned a default Worker response rather than the ACAC static reader.
- Recorded the next Cloudflare checks without adding secrets or private dashboard details.

검증:

- External HTTP checks were recorded in the Obsidian worklog and Daily pointer.
- This was an operational verification step without a repo commit.

## 01:34 — Remove Workers-Incompatible Redirects

Workers static assets rejected the generated `dist/_redirects` file because the Pages-style rewrite could loop.
The repo build was adjusted so Workers `not_found_handling` owns the app-shell fallback.

- Removed `dist/_redirects` generation from `scripts/build_trove.py`.
- Changed `scripts/check_public_payload.py` so `_redirects` fails the deploy check.
- Updated Cloudflare deploy references to explain why `_redirects` is not used for Workers.
- Created and pushed commit `e37eb34 Remove redirects from Workers static assets build`.

검증:

- `python3 scripts/deploy_check.py` passed with 19 public notes and 0 warnings.
- `npx wrangler deploy --dry-run` passed.
- Evidence was checked through Obsidian worklog, Daily pointer, `git log`, `git show --stat`, and `git show --name-only`.

## 01:52 — First Cloudflare Deployment Verified

The first Cloudflare Workers static assets deployment was verified from outside the local machine.
Both apex and www hostnames returned the ACAC static reader instead of GitHub Pages or a default Worker response.

- Verified `https://acac.sh/` returned HTTP 200 and ACAC HTML.
- Verified direct `/trove/<id>` and `/search` routes returned the app shell.
- Verified `data/build.json` and markdown payload routes were reachable.
- Confirmed live build metadata reported 19 public notes and 0 warnings.

검증:

- External HTTP checks were recorded in the Obsidian worklog and Daily pointer.
- This was an operational verification step without a repo commit.

## 01:59 — WWW Redirect Verified

The canonical redirect from `www.acac.sh` to apex `acac.sh` was verified.
The check confirmed that path and query string were preserved through the redirect.

- Verified `https://www.acac.sh/` redirects to `https://acac.sh/`.
- Verified `www` note routes redirect to the matching apex note route and return HTTP 200.
- Verified `www` search routes preserve query strings and return HTTP 200 after redirect.

검증:

- External HTTP checks were recorded in the Obsidian worklog and Daily pointer.
- This was an operational verification step without a repo commit.

## 02:01 — Reader Product UI Design System

- Reworked the static reader UI so the first screen feels like a product dashboard instead of a document landing page.
- Added design tokens, surface rules, status pills, reader typography, state cards, responsive drawer behavior, and search result states in `site/assets/style.css` and `site/assets/app.js`.
- Added a visible search submit control so mobile search does not depend only on Enter key behavior.
- Updated `scripts/build_trove.py` so `home.json` includes the latest Daily, Design, Decision, and Worklog pointers for the dashboard.
- Added [[first-instance-reader-design-system]] and linked it from the ACAC project index.
- Verified `python3 scripts/deploy_check.py`, desktop Home/Note/Search/Missing routes, mobile drawer, mobile search, and mobile Note/Missing routes.

## 02:11 — Workers Deploy Docs and Analytics Cleanup

- Updated README, active design docs, deploy references, and deploy decisions so the first live `acac.sh` deployment is treated as Cloudflare Workers static assets rather than Pages `_redirects`.
- Clarified that Cloudflare Web Analytics should prefer dashboard automatic setup for the proxied `acac.sh` hostname, while `ACAC_CF_WEB_ANALYTICS_TOKEN` remains empty unless manual beacon injection is explicitly chosen.
- Extended `data/build.json` analytics metadata so `analytics.enabled=false` means the repo did not inject a manual beacon, not that dashboard analytics has been proven disabled.
- Updated the reader Build status copy to show repo-injected manual beacon status and added a mobile drawer close button.
- Verified public `https://acac.sh/data/build.json` still reports `analytics.enabled=false`, and the current served HTML has no Cloudflare beacon script.
- Verified `python3 scripts/deploy_check.py`, local `/`, `/trove/N_TyCBWLFT`, `/search?q=Cloudflare`, missing note route, desktop layout, and mobile drawer behavior.

## 02:12 — Reader Tree Follow-Up Captured in Private Vault

Reader sidebar follow-up requirements were captured in the private project design area rather than in the public ACAC trove.
The public-safe record here keeps only the scope and boundary, not the private design body.

- Captured collapsible tree behavior, badge removal, compact chevron rows, and trove layer naming as follow-up requirements.
- Captured loading skeleton and relation map follow-up scope.
- Kept the detailed next-session prompt out of the public trove.

검증:

- Obsidian worklog and Daily pointer confirmed the private design note was the target record.
- No private design body was copied into public trove.

## 02:22 — Read-Write Editor Design Moved to Private Vault

The read-write editor and runtime integration design was moved out of public ACAC source.
The public reader stayed read-only, while the planning record remained in the private project design area.

- Captured browser editor, note lifecycle, proposal flow, local trust boundary, and runtime integration scope privately.
- Removed the mistaken public trove design note and related public pointers.
- Kept anonymous public write surface out of the ACAC reader.

검증:

- Obsidian worklog and Daily pointer confirmed the private-only design move.
- Public trove was checked so the private design body was not included.

## 02:22 — Trove Navigator and Relation Map Preview

- Reworked the public reader sidebar into a collapsible trove navigator with chevron-only rows, active-route folder expansion, and session-persistent open folders.
- Removed sidebar text badges such as `dir`, `day`, `des`, `dec`, `ref`, and `log` so titles have the main row space.
- Renamed the lower navigation layer to `Trove layers` and display `_config` / `_archived` as `Operating layer` / `Archive` in primary navigation labels.
- Added `data/graph.json` as a read-only static graph contract and rendered a bounded 1-hop relation map preview in the note context panel.
- Added compact text-free tree icons for folder, note, daily, decision, design, reference, worklog, operating, and archive rows without restoring text badges.
- Updated reader design docs for the new tree behavior, trove layer naming, loading skeleton width, and graph data contract.
- Verified `python3 scripts/deploy_check.py`, local `/`, `/trove/N_TyCBWLFT`, `/search?q=Cloudflare`, missing route, desktop tree expand/collapse, active folder auto-open, mobile drawer behavior, mobile note/search routes, and no horizontal overflow.

## 02:29 — Sidebar Icon Removal and Indent Tightening

- Removed folder and note icons from the sidebar tree so chevrons are the only tree affordance.
- Reduced nested tree indentation and row padding so deep items keep more title width.
- Verified `python3 scripts/deploy_check.py` and mobile drawer layout with `Common Agent Rules` visible without truncation.

## 02:31 — Note Header Badge Spacing

- Added note-header spacing so the metadata badge row no longer sits directly against the document title.
- Verified `python3 scripts/deploy_check.py` and measured a 12px gap on the `Claude Agent Entry` note header.

## 02:34 — Reader Navigator Completion and Source Write Contract Split

The reader navigator and relation map work was reviewed as a completed read-only reader change.
The next read-write work was split into a smaller local source-write service contract instead of opening a public editor.

- Confirmed the existing sidebar and graph changes matched the intended read-only scope.
- Reintroduced small text-free tree glyphs after the mobile clipping fix.
- Verified private design content was not present in the public payload.
- Captured source write service contract as the next small implementation boundary.

검증:

- `python3 scripts/deploy_check.py` passed.
- Local dist fallback routes were checked for home, note, search, missing, desktop, and mobile drawer behavior.
- Obsidian worklog, Daily pointer, and git history confirmed the split.

## 02:43 — Note Lifecycle Design and Source Write Service Skeleton

The note lifecycle design stayed private, while the first local-only implementation skeleton was added to the repo.
The public `acac.sh` reader did not gain a write API or editor entry.

- Added `scripts/source_write_service.py` with path safety checks, markdown inspection, dry-run diff previews, and validation/build wrappers.
- Added `tests/test_source_write_service.py`.
- Covered repo-outside paths, generated outputs, `_assets`, dry-run no-write behavior, and route ID guard behavior.
- Kept create, rename, move, archive, and delete as contracts or previews only.
- Created commit `d965904 Add local source write service skeleton`.

검증:

- `python3 -m unittest discover -s tests` passed.
- `python3 -m py_compile scripts/source_write_service.py tests/test_source_write_service.py` passed.
- `python3 scripts/source_write_service.py run-validation --skip-build` passed.
- `python3 scripts/deploy_check.py` passed.
- `rg` confirmed private design filenames were not present in the public repo.

## 02:44 — Reader Search Palette Verification

The reader search palette implementation was verified against the local `dist/` build.
This was a verification pass for an already committed search palette, not a new public write feature.

- Verified home, note, and search routes locally.
- Checked `Ctrl+K`, `Meta+K`, typing, highlight, arrow navigation, enter, and escape flows.
- Checked mobile palette opening from the header search button.
- Confirmed opening the palette closes the mobile drawer.
- Confirmed public search index entries were public-safe and excluded `_assets`.

검증:

- `python3 scripts/deploy_check.py` passed.
- Local `python3 scripts/serve_dist.py --port 4173` was used for route checks.
- Obsidian worklog, Daily pointer, and git commits `f1cff1a` and `c773fed` were checked.

## 02:46 — Live ACAC Deployment Verified

The search palette deployment was verified on live `acac.sh`.
A clean `origin/main` snapshot was used for deploy checks because the active worktree had unrelated UI changes.

- Verified live home, project note, and search routes returned HTTP 200.
- Confirmed live build metadata reported 20 public notes and 0 warnings.
- Confirmed live HTML/JS included search palette code.
- Confirmed live search index entries were public-safe and did not expose `_assets`.

검증:

- `python3 scripts/deploy_check.py` passed in the clean snapshot.
- `curl` checks against live ACAC routes and assets succeeded.
- Obsidian worklog and Daily pointer confirmed the live verification.

## 02:49 — Reader Loading and Sidebar Density Polish

- Reduced sidebar tree chrome again so chevron columns, nested padding, row padding, and row height take less space.
- Kept folder and note rows icon-free, with chevrons as the only tree affordance.
- Increased note metadata badge to title spacing so the document heading has clearer separation.
- Reworked loading skeleton markup so it uses the same narrow reader body width and starts at the same reader body height instead of a centered card offset.
- Updated the reader design system spacing guidance for dense tree rows and body-aligned loading states.
- Committed and pushed the reader polish as `93de099 Polish reader loading and sidebar spacing`.
- Confirmed the live Cloudflare build picked up the pushed reader polish.
- Verified `python3 scripts/deploy_check.py`, mobile tree computed spacing, and desktop `Claude Agent Entry` note header spacing.

## 03:13 — Note Lifecycle Dry-Run Previews Completed

The local-only source write service was expanded from skeleton to full dry-run previews for the planned note lifecycle.
This still did not expose a public editor, public API, MCP integration, hooks, or live Claude connection.

- Implemented create note dry-run preview.
- Split title rename and file path rename previews.
- Added move and archive previews with path guards.
- Added hard delete confirmation guard and warning behavior.
- Added CLI preview subcommands for create, rename, move, archive, and hard delete.
- Expanded tests to cover the lifecycle preview contract.
- Created and pushed commit `7e50da7 Implement note lifecycle dry-run previews`.

검증:

- `python3 -m unittest discover -s tests` passed.
- `python3 -m py_compile scripts/source_write_service.py tests/test_source_write_service.py` passed.
- `python3 scripts/source_write_service.py run-validation --skip-build` passed.
- Lifecycle preview CLI smoke tests passed.
- `python3 scripts/deploy_check.py` passed.
- `rg` confirmed private design filenames were not present in the public repo.

## 20:07 — Apply Create Local Source Write Implemented

The second source-write implementation step opened real local writes for create only.
Rename, move, archive, delete, browser editor, public API, and Cloudflare write surfaces stayed closed.

- Added `ApplyResult` for preview, validation result, applied files, rollback files, and errors.
- Implemented `apply_create_note` to create a trove markdown file and run validation.
- Added rollback behavior when validation fails and the created file is unchanged.
- Added `data/id-registry.json` path and note id duplicate guards.
- Added CLI `apply-create`.
- Expanded unit tests for successful apply, failed validation rollback, and duplicate ID guard.
- Created and pushed commit `7be044d Add local apply-create source write`.

검증:

- `python3 -m unittest discover -s tests` passed with 16 tests.
- `python3 -m py_compile scripts/source_write_service.py tests/test_source_write_service.py` passed.
- `python3 scripts/deploy_check.py` passed.
- `rg` confirmed private design filenames were not present in the public repo.

## 20:08 — Trove Note Convention

- Added [[trove-note-convention]] as the public-safe, ACAC-specific note-writing convention for `trove/**/*.md`.
- Linked the convention from the memory index and shared agent rules so future sessions can find it before editing notes.
- Normalized legacy design frontmatter to the current required ACAC fields and refreshed local frontmatter examples.
- Verified `python3 scripts/validate_trove.py`, `python3 scripts/build_trove.py`, and `python3 scripts/deploy_check.py`.

## 20:10 — README Current State Refresh

- Rewrote the root README around the current ACAC state: live `acac.sh` reader, `trove/` source, generated outputs, local commands, and Workers static assets deployment.
- Added direct project document links for the project index, architecture, reader design system, and Workers deploy prep reference.
- Verified `https://acac.sh/` returns HTTP 200 and `python3 scripts/deploy_check.py` passes with the current trove source.

## 20:11 — Active Session Worklog Audit

Active ACAC Codex threads were audited to see whether meaningful work was being logged in the same place.
The audit found that records were split between private Obsidian worklogs and repo-local trove worklogs.

- Checked ACAC-related Codex threads and recent final answers.
- Confirmed apply-create and source-write work had private Obsidian worklog records.
- Confirmed README and trove note convention work had repo-local trove worklog records.
- Confirmed a sidebar/FORGE-related thread intentionally avoided adding extra worklog during concurrent edits.
- Identified that `trove` should become the canonical worklog location going forward.

검증:

- Codex thread list and recent thread turns were checked.
- Obsidian worklog and Daily pointer confirmed the audit record.

## 20:12 — Desktop Sidebar Collapse Deployed

The public reader desktop sidebar gained collapse and expand behavior while preserving the mobile drawer close pattern.
The live deployment was checked through Cloudflare build metadata and static assets.

- Added a desktop sidebar collapse control.
- Collapsed state reduced the sidebar to a rail with brand mark and expand affordance.
- Stored the desktop collapse state in browser session storage.
- Kept the mobile drawer close button behavior separate.
- Confirmed live assets contained the sidebar collapse code and styles.

검증:

- `python3 scripts/deploy_check.py` passed.
- Live `https://acac.sh/data/build.json` showed a refreshed build timestamp.
- Live HTML/CSS/JS contained the collapse-related selectors and state key.

## 20:13 — FORGE Sidebar Label Deploy

The reader sidebar labels were updated so the UI could distinguish user-facing troves from the operating layer.
This was a UI label deploy, separate from the later in-progress source structure refactor.

- Updated the reader sidebar label surface around `TROVES` and `FORGE`.
- Adjusted sidebar HTML, CSS, and JavaScript together.
- Kept the change to the public reader UI layer without moving source folders in this completed deploy.
- Created commit `ad530eb Update reader sidebar forge labels`.

검증:

- Evidence was checked through `git log`, `git show --stat`, and `git show --name-only`.
- Codex thread list showed a separate FORGE structure refactor still in progress, so this worklog records only the completed label deploy.

## 20:22 — FORGE Source Structure Refactor

- Verified the pre-change baseline with `python3 scripts/deploy_check.py`; it passed before the FORGE refactor.
- Moved the operating layer from `trove/_config/` to `forge/_config/`, archive storage to `forge/_archived/`, and hidden storage to `forge/_assets/`.
- Kept canonical document routes as `/trove/<id>` for both TROVES and FORGE documents to avoid changing the deployed route contract.
- Updated validation, build, source-write helpers, generated tree/search/home data, and root agent entry sync to read both `trove/` and `forge/`.
- Updated README, project index, active design docs, the legacy principles reference, and generated `AGENTS.md` / `CLAUDE.md` for the new source boundary.
- Verified `python3 scripts/validate_trove.py`, `python3 scripts/build_trove.py`, and generated tree metadata with `forge/_config` shown as `Operating layer`.

## 20:29 — Trove Note Convention OKF Strengthening

- Expanded [[trove-note-convention]] from a thin checklist into an OKF-inspired source note convention.
- Added detailed rules for note types, evergreen behavior, filenames, Mermaid diagrams, wikilinks, indexes, related notes, backlinks, footnotes, public page footers, and commit workflow.
- Kept the convention public-safe by replacing private legacy infrastructure rules with ACAC `trove/` and `forge/` equivalents.
- Verified `python3 scripts/validate_trove.py`, `python3 scripts/build_trove.py`, and `python3 scripts/deploy_check.py`.

## 20:30 — Public Reader Sidebar Polish

The ACAC public reader sidebar was polished into a more product-grade trove navigator while keeping the read-only static reader boundary.
The pass focused on visual design only and did not add live runtime integration, MCP, hooks, or automatic memory sync.

- Compared the current ACAC sidebar with references for macOS-style soft controls, segmented selected states, developer docs hierarchy, and Tailwind-style section rhythm.
- Refined the sidebar brand mark, search surface, shortcut hint, search submit, collapse button, and mobile close button around one soft rounded control language.
- Added refined TROVES and FORGE section icon rules using product-style SVG icons while keeping file and folder rows icon-free for title width.
- Kept existing tree behavior intact: folder expand/collapse, active route folder auto-open, session storage open state, desktop collapse rail, and mobile drawer close.
- Updated [[first-instance-reader-design-system]] so future reader work keeps the same sidebar boundary.

검증:

- `python3 scripts/deploy_check.py` passed.
- Browser checks passed for desktop close button hidden, desktop collapse visible, section icons rendered, `_assets` hidden, folder expand/collapse, active route auto-open, collapsed rail, mobile drawer open, and mobile close behavior.

## 20:34 — Worklog Note Convention Setup

ACAC 작업 기록을 repo-local trove와 Daily에 일관되게 남기기 위한 규약을 만들었어요.
기존 note convention과 같은 memory convention 위치에 두고, agent 규칙에서 찾을 수 있게 연결했어요.

- [[worklog-note-convention]]을 `forge/_config/Memory/Conventions/`에 추가했어요.
- `forge/_config/Memory/MEMORY.md`에 worklog convention pointer를 추가했어요.
- `forge/_config/Agents/common.md`에 worklog 기록 규칙을 연결하고 `scripts/sync_agent_docs.py`로 `AGENTS.md`와 `CLAUDE.md`를 갱신했어요.
- `python3 scripts/build_trove.py`가 새 convention의 `id`를 부여하고 `data/id-registry.json`을 갱신했어요.
- 요청에 따라 convention 파일 stem과 wikilink를 `worklog-note-convention`으로 확정했어요.
- 이 convention 작업만으로는 deploy를 하지 않았어요.

검증:

- `python3 scripts/validate_trove.py` passed with 22 notes and 0 warnings.
- `python3 scripts/build_trove.py` passed with 22 public notes.
- `python3 scripts/deploy_check.py` passed.
- `git diff --check` passed.
- 작업 중 별도 thread로 보이는 `scripts/source_write_service.py`와 `tests/test_source_write_service.py` 변경은 되돌리지 않고 범위 밖 변경으로 분리했어요.

## 20:52 — Trove Note Convention Compaction

[[trove-note-convention]]을 한 노트로 유지하되 실행 규약처럼 읽히도록 압축했어요.
긴 설명을 줄이고 `Must Follow`, OKF profile, naming/linking, evidence/footer, commit workflow 순서로 재배치했어요.

- 기존 세부 규칙은 유지하되 중복된 설명과 긴 예시를 줄였어요.
- Mermaid, footnotes, public page footer, related notes, backlinks, filename, commit 규칙을 한 섹션 흐름으로 묶었어요.
- [[worklog-note-convention]]을 related note로 연결했어요.

검증:

- `python3 scripts/validate_trove.py` passed with 22 notes and 0 warnings.
- `python3 scripts/deploy_check.py` passed.
